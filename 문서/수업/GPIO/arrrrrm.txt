stm32f103rbt6

다운하면 플레쉬 코어가 끌고가져감
sram 변수저장 20k byte

dma diract memory access 
cpu 상관없이 실행
여러가지 작업 가능

1바이트 = 8비트 
32비트 구현가능 4g바이트

idr = pin
odr = port
in,out = crh, crl

gpioa 에서 핀을 output50, pushpull 설정
logic analyzer에서 porta.0  bit로 변경