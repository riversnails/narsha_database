1. STM32F103RBT6 ARM Processor 에서 10ms 주기의 100단계 PWM 을 
   만들려고 할 때 Timer2 에서
   업데이트 인터럽트(100번의 인터럽트)만 사용할 때 
   셋팅해야 하는 레지스터와 소스를 적으시오.
   그리고 10micros(Systick 이용) 이용해서 가변하시오(10 ms 마다).
   (레지스터 및 ISR 루틴, main 함수 안의 무한루프문)


2. 위의 문제에서 2ms 주기의 단계 100단계 PWM을 Timer2 의 
   업데이트/캡쳐컴페어 인터럽터를 사용해서 구현할 때
   셋팅해야 할 레지스터와 코드를 적으시오.
   그리고 10micros(Systick 이용) 이용해서 가변하시오(10 ms 마다).
   (레지스터 및 ISR 루틴, main 함수 안의 무한 루프)